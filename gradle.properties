# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -XX:MaxMetaspaceSize=512m
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app"s APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
# JitPack/AGP 8.2: suppress warning that compileSdk 35 is newer than tested
android.suppressUnsupportedCompileSdk=35
kinescopeSdkVersion=0.1.17

# Maven Central publishing (com.vanniktech.maven.publish) — no secrets here.
# Put credentials / GPG in ~/.gradle/gradle.properties (not in this repo).
GROUP=io.kinescope
VERSION_NAME=0.1.17
POM_INCEPTION_YEAR=2022
POM_URL=https://github.com/kinescope/kotlin-kinescope-player
POM_SCM_URL=https://github.com/kinescope/kotlin-kinescope-player
POM_SCM_CONNECTION=scm:git:git://github.com/kinescope/kotlin-kinescope-player.git
POM_SCM_DEV_CONNECTION=scm:git:ssh://git@github.com/kinescope/kotlin-kinescope-player.git
POM_LICENCE_NAME=The Apache Software License, Version 2.0
POM_LICENCE_URL=https://www.apache.org/licenses/LICENSE-2.0.txt
POM_LICENCE_DIST=repo
POM_DEVELOPER_ID=kinescope
POM_DEVELOPER_NAME=Kinescope
POM_DEVELOPER_URL=https://kinescope.io
mavenCentralPublishing=true
signAllPublications=true
# Set true only when you want Portal to release automatically after upload validation:
# mavenCentralAutomaticPublishing=true
